export type RegisterEventDto = {
	eventId: string;
};

export type UnregisterEventDto = {
	eventId: string;
};

export type AttendEventDto = {
	eventId: string;
};

export type UnattendEventDto = {
	eventId: string;
};
